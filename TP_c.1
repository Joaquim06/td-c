#include <stdlib.h>
#include <stdio.h>

// définition de macros
// #define DEBUG
// programme principal

// Définition de types
typedef unsigned char boolean;
typedef unsigned char byte;

int main()
{
	// Déclaration de variables réservées au main(), et initialisations facultatives
	char OneLetter = 'A';
	char AnOtherLetter = 66; // Le code ASCII en décimal de la lettre B
	byte toto = 0;
	unsigned char VerySmallCounter = 255;
	short SmallCounter = 32767;
	int Counter = 0;
	long LargeCounter = 2147483647;
	unsigned long FullRangeLargeCounter = 4294967295;
	int ChienDeGarde = 10;
	float Dim1 = 1.414F;
	double Dim2 = .5L;
	boolean TestResult = 1;
	// Déclaration de constante
	const double Pi = 3.14159265L;
	// les instructions du programme principal : main()
	/********** Debut de programme *****************/
	// Formats Microsoft C : https://docs.microsoft.com/fr-fr/cpp/c-runtime-library/format-specification-syntax-printf-and-wprintf-functions?view=vs-2019
	printf("\n");
	printf("Oneletter = %c, de longueur %d octets\n", OneLetter, sizeof(OneLetter));
	printf("AnOtherLetter = %c, de longueur %d octets\n", AnOtherLetter, sizeof(AnOtherLetter));
	printf("AnOtherLetter = %x, de longueur %d octets\n", AnOtherLetter, sizeof(AnOtherLetter));
	printf("Toto de type byte = %c, de longueur %d octets\n", toto, sizeof(toto));
	printf("Counter = %d, de longueur %d octets\n", Counter, sizeof(Counter));
	printf("LargeCounter = %d, de longueur %d octets\n", LargeCounter, sizeof(LargeCounter));
	printf("FullRangeLargeCounter = %u, de longueur %d octets\n", FullRangeLargeCounter, sizeof(FullRangeLargeCounter));
	printf("ChienDeGarde = %d, de longueur %d octets\n", ChienDeGarde, sizeof(ChienDeGarde));
	printf("Dim1 = %.3f, de longueur %d octets\n", Dim1, sizeof(Dim1));
	printf("Dim2 = %d, de longueur %d octets\n", Dim2, sizeof(Dim2));
	printf("TestResult de type boolean = %d, de longueur %d octets\n", TestResult, sizeof(TestResult));
	printf("Pi = %.8f, de longueur %d octets\n", Pi, sizeof(Pi));
	/*********** Fin de programme ******************/
	//system("pause"); // Ne fonctionne pas sur MAC
	return (EXIT_SUCCESS);
}
